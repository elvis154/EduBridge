Frontend (React + Vite + Tailwind CSS)

Setup:
1. cd frontend
2. npm install
3. npm run dev

The frontend uses Tailwind CSS for styling.

The app expects the backend to run at http://localhost:8000 by default.
